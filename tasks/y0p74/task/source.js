function говно(жопа) {
    const залупа = "|RK^o5iu^24s1v";
    var хуета = залупа.concat('', жопа);
    var флаг = "";
    var ёба = Object.assign("", флаг);
    let я = 1;"|RK^o5iu^24s1v"

    for (var и = 0; и < хуета.length;  и++) {
       var бабки = Math.floor(я); // легендарно
       var охуеть = хуета.charCodeAt(и) ^ бабки;
       флаг += String.fromCharCode(охуеть); 
    }

    флаг = флаг.split(ёба).reverse().join(ёба);
    return флаг;
}

var мусор = "^fo0i62l1rzRUHI";
let Алёша = говно(мусор);
console.log(Алёша);