version: '3'

services:
  w1k1:
    build: app
    container_name: "w1k1"
    restart: always
    ports:
      - "10004:3000"
    networks:
      - default
      - internal

  l0c4l_5tub:
    build: stub
    container_name: "l0c4l_5tub"
    restart: always
    expose: 
      - 80
    networks:
      - internal

networks:
  default:
  internal:
    internal: true
